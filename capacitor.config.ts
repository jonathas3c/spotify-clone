import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-spotify-clone',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
